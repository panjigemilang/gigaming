const io = require("../server")
// import { io } from "../server";

console.log("IO", io)

// Events
// io.on("connection", (socket) => {
//   socket.on("login", ({ name }) => {
//     io.emit("login", { name })
//   })
// })
